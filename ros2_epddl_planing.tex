\documentclass[12pt,a4paper]{article}

\usepackage[utf8]{inputenc}
\usepackage[T1]{fontenc}
\usepackage[english]{babel}
\usepackage{geometry}
\geometry{a4paper, margin=2.5cm}

\usepackage{amsmath, amssymb, amsthm}
\usepackage{mathtools}
\usepackage{graphicx}
\usepackage{xcolor}
\usepackage{hyperref}
\usepackage{booktabs}
\usepackage{array}
\usepackage{tabularx}
\usepackage{multirow}
\usepackage{listings}
\usepackage{caption}
\usepackage{subcaption}
\usepackage{float}
\usepackage{enumitem}
\usepackage{csquotes}
\usepackage{setspace}
\usepackage{parskip}
\usepackage{fancyhdr}
\usepackage{titlesec}
\usepackage{microtype}
\usepackage{cite}
\usepackage{algorithm}
\usepackage{algpseudocode}
\usepackage{tikz}
\usetikzlibrary{shapes.geometric, arrows.meta, positioning, fit, backgrounds, calc,
                decorations.pathreplacing, decorations.markings}
\usepackage{pgfplots}
\pgfplotsset{compat=1.18}

% ---- Color definitions ----
\definecolor{darkblue}{RGB}{0,51,102}
\definecolor{medblue}{RGB}{0,102,153}
\definecolor{lightblue}{RGB}{173,216,230}
\definecolor{darkgray}{RGB}{50,50,50}
\definecolor{codegray}{RGB}{245,245,245}
\definecolor{codegreen}{RGB}{0,128,0}
\definecolor{codepurple}{RGB}{128,0,128}
\definecolor{codered}{RGB}{160,0,0}
\definecolor{lightyellow}{RGB}{255,253,230}
\definecolor{lightgreen}{RGB}{230,255,230}

\hypersetup{
    colorlinks=true,
    linkcolor=darkblue,
    citecolor=medblue,
    urlcolor=medblue,
    pdftitle={ROS2 PlanSys2 and Task Planning: Preliminary Research for DEL-based Multi-Agent Planning},
    pdfauthor={Preliminary Research Document},
}

\pagestyle{fancy}
\fancyhf{}
\fancyhead[L]{\small\textcolor{darkblue}{\textit{Preliminary Research: DEL-based Task Planning for Multi-Agent Robotics}}}
\fancyhead[R]{\small\thepage}
\fancyfoot[C]{\small\textcolor{darkgray}{\hrule\vspace{2pt}ROS2 | PlanSys2 | EPDDL | Multi-Agent Systems}}
\renewcommand{\headrulewidth}{0.5pt}

\lstdefinestyle{ros2style}{
    backgroundcolor=\color{codegray},
    basicstyle=\ttfamily\footnotesize,
    keywordstyle=\color{darkblue}\bfseries,
    commentstyle=\color{codegreen}\itshape,
    stringstyle=\color{codered},
    numberstyle=\tiny\color{darkgray},
    numbers=left,
    breaklines=true,
    breakatwhitespace=false,
    frame=single,
    rulecolor=\color{darkgray},
    tabsize=2,
    captionpos=b,
    showstringspaces=false,
    morekeywords={action, node, launch, package, executor, domain, problem, predicate,
                  auto, nullptr, std, make_shared, shared_ptr, string, int, bool, void,
                  return, class, public, private, protected, override, virtual, const,
                  using, namespace, include, rclcpp, rclcpp_action}
}
\lstset{style=ros2style}

\theoremstyle{definition}
\newtheorem{definition}{Definition}[section]
\newtheorem{example}{Example}[section]
\theoremstyle{plain}
\newtheorem{proposition}{Proposition}[section]
\newtheorem{remark}{Remark}[section]

\titleformat{\section}{\large\bfseries\color{darkblue}}{\thesection}{1em}{}[\color{medblue}\titlerule]
\titleformat{\subsection}{\normalsize\bfseries\color{darkblue}}{\thesubsection}{1em}{}
\titleformat{\subsubsection}{\normalsize\itshape\color{darkgray}}{\thesubsubsection}{1em}{}

\setlength{\parindent}{0pt}
\setlength{\parskip}{8pt}
\onehalfspacing

\begin{document}


\begin{titlepage}
\begin{center}
\vspace*{1.5cm}
{\color{darkblue}\rule{\linewidth}{1.5pt}}
\vspace{0.5cm}

{\LARGE\bfseries\color{darkblue} ROS2 PlanSys2 and Task Planning for Multiple Robots:\\[0.4em]
\large\color{darkgray} Preliminary Research Toward DEL-based Task Planning\\
with EPDDL for Multi-Agent Robotics in Uncertain Environments}

\vspace{0.5cm}
{\color{darkblue}\rule{\linewidth}{1.5pt}}
\vspace{1.5cm}

{\large Preliminary Research Document}\\[0.5em]
{\normalsize Preliminary research in the context of epistemic multi-agent planning,\\
grounded in Dynamic Epistemic Logic and the EPDDL framework}

\vspace{2cm}
\begin{tabular}{rl}
\textbf{Research Area:} & Autonomous Robotics \& Epistemic Planning \\
\textbf{Keywords:}      & ROS2, PlanSys2, PDDL, DEL, EPDDL, Multi-Robot Systems \\
\textbf{Document Type:} & Preliminary Research / State of the Art \\
\textbf{Version:}       & 1.0 \\
\end{tabular}

\vspace{3cm}
{\color{darkgray}\textit{Haniel Ulises Vásquez Morales}}

\vfill
{\small\color{darkgray} This document is a preliminary academic survey compiled in preparation for research on
DEL-based epistemic task planning using EPDDL for multi-agent robotic systems operating under
partial observability. It is not a finished publication.}
\end{center}
\end{titlepage}


\newpage
\begin{abstract}
\noindent
The increasing deployment of autonomous robotic systems in complex, uncertain, and partially
observable environments demands planning formalisms that transcend the capabilities of classical
automated planning. This document constitutes a preliminary research survey covering two major
pillars of the target research agenda: (i)~the state of the art in robot task planning using the
Robot Operating System 2 (ROS2), with particular focus on the PlanSys2 framework and its
PDDL-based planning infrastructure; and (ii)~the theoretical and conceptual foundations of Dynamic
Epistemic Logic (DEL) and the Epistemic Planning Domain Definition Language (EPDDL), as introduced
by Burigana and Fabiano for IPC 2026. The ultimate motivation of this survey is to chart a research
path toward a DEL-based multi-agent task planning system for heterogeneous robot fleets operating
under epistemic uncertainty. We discuss the architecture of ROS2 multi-robot systems, the structure
and limitations of existing PDDL-based planners in robotics, the semantics of epistemic states and
actions in DEL, and the syntactic mechanisms of EPDDL. We conclude with an analysis of the
integration challenges and open research questions at the intersection of these fields.
\end{abstract}

\vspace{1em}
\noindent\textbf{Keywords:} ROS2, PlanSys2, PDDL, task planning, multi-robot systems, Dynamic
Epistemic Logic, epistemic planning, EPDDL, partial observability, multi-agent systems.

\newpage
\tableofcontents


\newpage
\section{Introduction}
\label{sec:intro}


Modern autonomous robotic systems are increasingly expected to operate in environments that are
shared with other agents---whether human operators, other autonomous robots, or dynamically
changing physical conditions---where the state of the world is not fully and reliably observable.
A warehouse robot fleet, a team of search-and-rescue drones, or a collaborative assembly line all
require their constituent agents to reason not only about what is true in the world, but also about
what each agent \textit{knows}, what each agent \textit{believes}, and what each agent knows or
believes about the epistemic states of others. Classical task planning formalisms, such as PDDL
(Planning Domain Definition Language), remain the de facto standard in practical robotic systems
and are supported by major middleware frameworks such as ROS2 through tools like PlanSys2.
However, classical planning fundamentally assumes a fully observable, deterministic environment
and a single omniscient planning agent---assumptions that are routinely violated in realistic
multi-robot deployments.

The objective of this document is to establish a comprehensive preliminary understanding of two
converging research areas: the engineering and middleware infrastructure for task planning in ROS2
multi-robot systems, and the theoretical framework provided by Dynamic Epistemic Logic (DEL) and
its associated language, EPDDL, for epistemic multi-agent planning. Together, these two pillars
form the foundation upon which a future system can be built: one capable of reasoning about the
knowledge and beliefs of individual robots, performing multi-agent coordination under partial
observability, and expressing rich epistemic goals such as ``robot $A$ knows whether corridor $c$
is blocked,'' or ``it is common knowledge among all robots that the package has been delivered.''

Section~\ref{sec:ros2} introduces ROS2 as a middleware architecture, covering its communication
model, multi-robot support, and lifecycle management. Section~\ref{sec:plansys2} examines PlanSys2,
the primary task planning framework for ROS2, including its PDDL integration, executor architecture,
and a concrete worked example using ROS2 Humble/Jazzy C++. Section~\ref{sec:multirobot} surveys
the state of the art in multi-robot coordination and planning within ROS2. Section~\ref{sec:del}
provides an accessible but technically grounded introduction to Dynamic Epistemic Logic.
Section~\ref{sec:epddl} presents EPDDL, its syntax, and action type libraries.
Section~\ref{sec:integration} analyzes the integration challenges and proposes a conceptual
architecture for a DEL-based ROS2 planning system. Section~\ref{sec:conclusion} summarizes
findings and outlines open research questions.


\newpage
\section{ROS2: Architecture and Multi-Robot Infrastructure}
\label{sec:ros2}


\subsection{Overview and Design Principles}

The Robot Operating System 2 (ROS2) is an open-source middleware framework designed to provide a
flexible and modular infrastructure for robotic software
development~\cite{macenski2022robot}. Unlike its predecessor ROS1, which relied on a centralized
master node, ROS2 is built atop the Data Distribution Service (DDS) standard, providing a fully
decentralized, quality-of-service-aware publish-subscribe communication layer. This architectural
shift makes ROS2 fundamentally more suited to multi-robot deployments, real-time systems, and
industrial applications.

The core communication abstractions in ROS2 are \textit{nodes}, \textit{topics},
\textit{services}, and \textit{actions}. Topics implement an asynchronous, many-to-many
publish-subscribe pattern. Services implement synchronous request-reply communication. Actions
extend services with long-running asynchronous goals, periodic feedback, and cancellation.

\begin{figure}[H]
\centering
\begin{tikzpicture}[
    block/.style={
        rectangle, draw=darkblue, fill=lightblue!40, rounded corners=4pt,
        minimum width=3.0cm, minimum height=1.0cm,
        font=\small, align=center, inner sep=6pt
    },
    layer/.style={
        rectangle, draw=medblue, dashed, fill=blue!5,
        rounded corners=6pt, inner sep=12pt
    },
    arrow/.style={-{Stealth[length=5pt]}, thick, color=darkblue},
]
\node[block] (nav)   at (0.0, 3.2) {Navigation\\Nav2};
\node[block] (manip) at (3.6, 3.2) {Manipulation\\MoveIt2};
\node[block] (perc)  at (7.2, 3.2) {Perception\\Sensors};

\node[block] (bt)    at (0.0, 1.2) {Behavior\\Tree};
\node[block] (plan)  at (3.6, 1.2) {PlanSys2\\Task Planner};
\node[block] (exec)  at (7.2, 1.2) {Executor\\Controller};

\node[block, minimum width=8.6cm] (dds) at (3.6, -0.5)
    {DDS Middleware (rmw layer)};

\begin{scope}[on background layer]
  \node[layer, fit=(nav)(manip)(perc),
        label={[font=\small\bfseries, yshift=4pt]above:Application Layer}]
        (applayer) {};
  \node[layer, fit=(bt)(plan)(exec),
        label={[font=\small\bfseries, yshift=4pt]above:Planning \& Control Layer}]
        (planlayer) {};
\end{scope}

\draw[arrow] (plan) -- (bt);
\draw[arrow] (plan) -- (exec);
\draw[arrow] (bt)   -- (nav);
\draw[arrow] (bt)   -- (manip);
\draw[arrow] (exec) -- (perc);

\draw[arrow] (applayer.south) -- (planlayer.north);
\draw[arrow, dashed] (planlayer.south) -- (dds.north);
\end{tikzpicture}
\caption{Simplified layered architecture of a ROS2 robot system. PlanSys2 operates within the
Planning \& Control Layer, interfacing with navigation, manipulation, and perception via behavior
trees and action servers, all underpinned by DDS.}
\label{fig:ros2arch}
\end{figure}

\subsection{DDS and the Communication Model}

DDS implements a data-centric publish-subscribe model where data flows between publishers and
subscribers through a shared data space---the \textit{Global Data Space}---without a centralized
broker. Quality of Service (QoS) policies govern reliability, durability, deadline, and liveness.
For multi-robot systems, DDS offers native distributed discovery: robots on the same network
segment automatically discover each other without manual configuration, mediated through the
\textit{DDS Domain} abstraction that partitions communication into independent contexts.

\subsection{Node Lifecycle and Managed Nodes}

ROS2 introduced a formal node lifecycle management protocol with states (Unconfigured, Inactive,
Active, Finalized) and defined transitions. From a task planning perspective, the lifecycle model
provides a principled mechanism for activating and deactivating capabilities on demand, enabling a
planning system to activate a navigation capability only when an action requiring it is dispatched.

\subsection{Multi-Robot Namespacing and Coordination}

ROS2 addresses multi-robot naming through a hierarchical namespace mechanism. Each robot is
assigned a unique namespace prefix (e.g., \texttt{/robot1}, \texttt{/robot2}), and all its nodes,
topics, services, and actions are scoped under that prefix. This allows multiple robot instances to
coexist on the same DDS domain without topic collisions and enables a centralized planning node to
address individual robots by namespace. Namespacing alone is, however, insufficient for true
multi-robot coordination, which additionally requires shared state representation, inter-robot
protocols, and synchronized action dispatch.

\subsection{ROS2 Actions and Task Execution}

The ROS2 action protocol is the primary mechanism for integrating task planning with robot
execution. An action server accepts goal requests from action clients, provides periodic feedback,
and returns a final result. This maps directly to the classical planning loop: the planner emits
a sequence of actions, each dispatched as a goal to the corresponding action server, with
execution monitored by the planner or a behavior executive. Action state management
(ACCEPTED, EXECUTING, CANCELING, SUCCEEDED, ABORTED, CANCELED) is handled transparently by
the ROS2 action infrastructure.


\newpage
\section{PlanSys2: Task Planning in ROS2}
\label{sec:plansys2}


\subsection{Architecture Overview}

PlanSys2 is the primary open-source framework for PDDL-based task planning in
ROS2~\cite{martin2021plansys2}. It provides a modular, multi-node architecture integrating
domain and problem management, plan generation via external PDDL solvers, and plan execution
via a behavior-tree-based action dispatcher.


\begin{figure}[H]
\centering
\begin{tikzpicture}[
    box/.style={
        rectangle, draw=darkblue, fill=lightblue!30, rounded corners=4pt,
        minimum width=3.0cm, minimum height=1.0cm,
        font=\small\bfseries, align=center, inner sep=6pt
    },
    ext/.style={
        rectangle, draw=codegreen!80!black, fill=codegreen!10, rounded corners=4pt,
        minimum width=2.8cm, minimum height=0.9cm,
        font=\small, align=center, inner sep=5pt
    },
    arr/.style={-{Stealth[length=5pt]}, thick, darkblue},
    darr/.style={-{Stealth[length=5pt]}, thick, darkgray, dashed},
    lbl/.style={font=\tiny\itshape, darkgray}
]
\node[box] (dm)  at (0.0,  0.0) {Domain\\Manager};
\node[box] (pm)  at (3.8,  0.0) {Problem\\Manager};
\node[box] (pl)  at (7.6,  0.0) {Planner\\Manager};

\node[box] (ex)  at (3.8, -2.4) {Executor\\Manager};

\node[ext] (client) at (-2.8, 0.0)  {Client\\Terminal / API};
\node[ext] (solver) at (11.0, 0.0)  {POPF / TFD\\Solver};
\node[ext] (bt)     at (3.8,  -4.6) {BehaviorTree.CPP\\Action Nodes};

\draw[arr] (client)  -- (dm)  node[midway,above,lbl]{srv/topics};
\draw[arr] (dm)      -- (pm)  node[midway,above,lbl]{domain};
\draw[arr] (pm)      -- (pl)  node[midway,above,lbl]{problem};
\draw[arr] (pl)      -- (solver) node[midway,above,lbl]{call};
\draw[arr] (solver.south) -- ++(0,-0.65) -| (pl.east)
           node[near start,right,lbl]{plan};

\draw[arr] (client.south) -- ++(0,-1.2) -- ++(2.8,0) -- (pm.west |- ex.north)
           -- ++(0,0.8) -- (pm.west)   node[near start, below, lbl]{srv/topics};

\draw[arr] (pl.south) -- ++(0,-0.6) -- (ex.east |- pl.south) -- (ex.east)
           node[near end, right, lbl]{plan};

\draw[arr]  (ex)    -- (bt)     node[midway,right,lbl]{dispatch};
\draw[darr] (bt.west) -- ++(-2.2,0) |- (ex.west)
            node[near start,above,lbl]{feedback};
\end{tikzpicture}
\caption{Internal architecture of PlanSys2. Domain Manager, Problem Manager, Planner Manager,
and Executor Manager are separate ROS2 nodes. An external solver (POPF/TFD) is called by the
Planner Manager, and execution is carried out through BehaviorTree.CPP action nodes.}
\label{fig:plansys2arch}
\end{figure}

PlanSys2 decouples domain management, problem management, planning, and execution into separate
ROS2 nodes following the microservice principle. Each component can be independently replaced,
updated, or scaled.

\subsection{Domain and Problem Management}

The Domain Manager maintains the PDDL domain specification. The Problem Manager maintains the
dynamic planning problem---the current set of ground predicates (the world state), instantiated
objects, and the current goal specification---and is the primary interface through which the robot
system updates its world model as perception events are received. Both managers expose ROS2
services and topics, allowing any node to add/remove predicates, objects, and update goals.

\subsection{Plan Generation and PDDL Solvers}

PlanSys2 delegates planning to external PDDL planners via a plugin architecture. The most
commonly used solvers are POPF~\cite{coles2010forward} and TFD (Temporal Fast Downward), both
supporting temporal PDDL with durative actions. The planning loop is triggered by a goal update
or world state change that renders the current plan invalid: the system serializes the domain and
problem, calls the solver, receives the plan, and passes it to the Executor Manager.

\subsection{Execution via Behavior Trees}

Plan execution in PlanSys2 is mediated by the BehaviorTree.CPP
library~\cite{faconti2018behaviortree}. Each PDDL action is associated with a behavior tree node
implementing execution logic, pre-condition verification, and post-condition assertion. The
Executor Manager constructs a behavior tree from the plan and runs it, monitoring each action for
success, failure, or timeout. The behavior tree is constructed statically from the plan; replanning
must be explicitly triggered when execution deviates from expectations.


\subsection{A Practical PlanSys2 Task: Package Transport in ROS2 Humble/Jazzy}
\label{subsec:plansys2example}


To ground the preceding architectural description concretely, we walk through a complete
PlanSys2 task package for ROS2 Humble (or Jazzy) targeting a logistics scenario: a single mobile
robot must transport two packages between three named locations in a warehouse. We cover the
scenario, the package structure, the PDDL domain and problem files, the C++ action executor
implementations, the CMake build configuration, the launch file, and the expected execution
behavior---illustrated with diagrams.

\subsubsection{Scenario Description}

\begin{figure}[H]
\centering
\begin{tikzpicture}[
    room/.style={
        rectangle, draw=darkblue!70, fill=lightblue!20, rounded corners=6pt,
        minimum width=3.0cm, minimum height=1.6cm,
        font=\small\bfseries, align=center, inner sep=8pt
    },
    robot/.style={
        circle, draw=darkblue, fill=darkblue!80, text=white,
        minimum size=0.85cm, font=\small\bfseries
    },
    pkg/.style={
        rectangle, draw=codered!80, fill=codered!15, rounded corners=3pt,
        minimum width=1.1cm, minimum height=0.65cm,
        font=\scriptsize, align=center, inner sep=3pt
    },
    pkggoal/.style={
        rectangle, draw=codegreen!80, fill=codegreen!15, rounded corners=3pt,
        minimum width=1.1cm, minimum height=0.65cm,
        font=\scriptsize, align=center, inner sep=3pt
    },
    conn/.style={-{Stealth[length=5pt]}, thick, darkgray},
    lbl/.style={font=\scriptsize, darkgray}
]

\node[room] (wh) at (0,   0) {Warehouse\\(start)};
\node[room] (lb) at (5.0, 0) {Loading\\Bay};
\node[room] (st) at (10.0,0) {Storage};

\draw[conn] (wh.east) -- (lb.west) node[midway, above, lbl]{navigate};
\draw[conn] (lb.west) -- (wh.east) node[midway, below, lbl]{return};
\draw[conn] (lb.east) -- (st.west) node[midway, above, lbl]{navigate};
\draw[conn] (st.west) -- (lb.east) node[midway, below, lbl]{return};

\node[robot] (r) at (-0.6, 1.4) {R};

\node[pkg] (p1) at ( 0.4, -1.4) {P1};
\node[pkg] (p2) at (-0.4, -1.4) {P2};
\node[font=\scriptsize, darkgray] at (0.0, -2.0) {Initial: Robot + P1 + P2};

\node[pkggoal] at (5.0,  -1.4) {P2\\\textit{goal}};
\node[font=\scriptsize, darkgray] at (5.0, -2.0) {Goal: P2 here};

\node[pkggoal] at (10.0, -1.4) {P1\\\textit{goal}};
\node[font=\scriptsize, darkgray] at (10.0,-2.0) {Goal: P1 here};
\end{tikzpicture}
\caption{Warehouse scenario: the robot (R) starts at the Warehouse; P1 and P2 are both at the
Warehouse. The goal is to deliver P1 to Storage and P2 to the Loading Bay.}
\label{fig:scenario}
\end{figure}

The environment has three locations---\texttt{warehouse}, \texttt{loading\_bay}, and
\texttt{storage}---connected bidirectionally. The robot begins at the warehouse. Both packages
begin at the warehouse. The plan requires picking up each package, navigating to its destination,
dropping it off, and returning to the start.

\subsubsection{Package Structure}

The task is implemented as a standard ROS2 package (compatible with both Humble and Jazzy). The
directory layout is as follows.

\begin{lstlisting}[language={}, caption={Package directory structure for the transport task.},
    label=lst:pkgtree]
transport_task/
|-- CMakeLists.txt
|-- package.xml
|-- pddl/
|   |-- transport_domain.pddl
|   `-- transport_problem.pddl
|-- src/
|   |-- navigate_action_node.cpp
|   |-- pick_up_action_node.cpp
|   `-- drop_off_action_node.cpp
|-- launch/
|   `-- transport_task.launch.py
`-- config/
    `-- plansys2_params.yaml
\end{lstlisting}

\subsubsection{PDDL Domain}

\begin{lstlisting}[language={}, caption={PDDL domain for the warehouse transport task
(\texttt{pddl/transport\_domain.pddl}).}, label=lst:pddldomain]
(define (domain transport)
  (:requirements :strips :typing :durative-actions :negative-preconditions)

  (:types
    robot location package
  )

  (:predicates
    (robot-at ?r - robot ?l - location)
    (package-at ?p - package ?l - location)
    (carrying ?r - robot ?p - package)
    (free ?r - robot)
    (connected ?from - location ?to - location)
  )

  (:durative-action navigate
    :parameters (?r - robot ?from - location ?to - location)
    :duration (= ?duration 10)
    :condition (and
      (at start (robot-at ?r ?from))
      (at start (connected ?from ?to))
      (over all (free ?r))
    )
    :effect (and
      (at start (not (robot-at ?r ?from)))
      (at end   (robot-at ?r ?to))
    )
  )

  (:durative-action pick-up
    :parameters (?r - robot ?p - package ?l - location)
    :duration (= ?duration 5)
    :condition (and
      (at start (robot-at ?r ?l))
      (at start (package-at ?p ?l))
      (at start (free ?r))
    )
    :effect (and
      (at start (not (package-at ?p ?l)))
      (at start (not (free ?r)))
      (at end   (carrying ?r ?p))
    )
  )

  (:durative-action drop-off
    :parameters (?r - robot ?p - package ?l - location)
    :duration (= ?duration 5)
    :condition (and
      (at start (robot-at ?r ?l))
      (at start (carrying ?r ?p))
    )
    :effect (and
      (at start (not (carrying ?r ?p)))
      (at end   (package-at ?p ?l))
      (at end   (free ?r))
    )
  )
)
\end{lstlisting}

\subsubsection{PDDL Problem}

\begin{lstlisting}[language={}, caption={PDDL problem instance
(\texttt{pddl/transport\_problem.pddl}).}, label=lst:pddlproblem]
(define (problem transport-1)
  (:domain transport)

  (:objects
    robot1                           - robot
    warehouse loading-bay storage    - location
    parcel1 parcel2                  - package
  )

  (:init
    (robot-at robot1 warehouse)
    (package-at parcel1 warehouse)
    (package-at parcel2 warehouse)
    (free robot1)
    (connected warehouse loading-bay)
    (connected loading-bay warehouse)
    (connected loading-bay storage)
    (connected storage loading-bay)
  )

  (:goal (and
    (package-at parcel1 storage)
    (package-at parcel2 loading-bay)
    (robot-at robot1 warehouse)
  ))
)
\end{lstlisting}

\subsubsection{C++ Action Executors}

Each durative action in the PDDL domain corresponds to a C++ class implementing
\texttt{plansys2::ActionExecutorClient}. The following listings show the three executors. All
three follow the same pattern: on activation they simulate the action with a progress timer, and
on success they call \texttt{finish(true, 1.0, "succeeded")}. In a physical deployment the
timer would be replaced by actual hardware calls (Nav2 goals, gripper commands, etc.).

\begin{lstlisting}[language=C++, caption={Navigate action executor
(\texttt{src/navigate\_action\_node.cpp}).}, label=lst:navigate]
#include <memory>
#include <string>
#include "plansys2_executor/ActionExecutorClient.hpp"
#include "rclcpp/rclcpp.hpp"
#include "rclcpp_lifecycle/lifecycle_node.hpp"

using namespace std::chrono_literals;

class NavigateAction : public plansys2::ActionExecutorClient
{
public:
  NavigateAction() : plansys2::ActionExecutorClient("navigate", 250ms)
  {
    progress_ = 0.0f;
  }

private:
  void do_work() override
  {
    if (progress_ < 1.0f) {
      progress_ += 0.05f;
      send_feedback(progress_, "navigating...");
    } else {
      finish(true, 1.0f, "navigation completed");
      progress_ = 0.0f;
    }
  }

  float progress_;
};

int main(int argc, char ** argv)
{
  rclcpp::init(argc, argv);
  auto node = std::make_shared<NavigateAction>();
  node->set_parameter(rclcpp::Parameter("action_name", "navigate"));
  node->trigger_transition(
    lifecycle_msgs::msg::Transition::TRANSITION_CONFIGURE);
  node->trigger_transition(
    lifecycle_msgs::msg::Transition::TRANSITION_ACTIVATE);
  rclcpp::spin(node->get_node_base_interface());
  rclcpp::shutdown();
  return 0;
}
\end{lstlisting}

\begin{lstlisting}[language=C++, caption={Pick-up action executor
(\texttt{src/pick\_up\_action\_node.cpp}).}, label=lst:pickup]
#include <memory>
#include <string>
#include "plansys2_executor/ActionExecutorClient.hpp"
#include "rclcpp/rclcpp.hpp"
#include "rclcpp_lifecycle/lifecycle_node.hpp"

using namespace std::chrono_literals;

class PickUpAction : public plansys2::ActionExecutorClient
{
public:
  PickUpAction() : plansys2::ActionExecutorClient("pick-up", 250ms)
  {
    progress_ = 0.0f;
  }

private:
  void do_work() override
  {
    if (progress_ < 1.0f) {
      progress_ += 0.10f;
      send_feedback(progress_, "picking up package...");
    } else {
      finish(true, 1.0f, "pick-up completed");
      progress_ = 0.0f;
    }
  }

  float progress_;
};

int main(int argc, char ** argv)
{
  rclcpp::init(argc, argv);
  auto node = std::make_shared<PickUpAction>();
  node->set_parameter(rclcpp::Parameter("action_name", "pick-up"));
  node->trigger_transition(
    lifecycle_msgs::msg::Transition::TRANSITION_CONFIGURE);
  node->trigger_transition(
    lifecycle_msgs::msg::Transition::TRANSITION_ACTIVATE);
  rclcpp::spin(node->get_node_base_interface());
  rclcpp::shutdown();
  return 0;
}
\end{lstlisting}

\begin{lstlisting}[language=C++, caption={Drop-off action executor
(\texttt{src/drop\_off\_action\_node.cpp}).}, label=lst:dropoff]
#include <memory>
#include <string>
#include "plansys2_executor/ActionExecutorClient.hpp"
#include "rclcpp/rclcpp.hpp"
#include "rclcpp_lifecycle/lifecycle_node.hpp"

using namespace std::chrono_literals;

class DropOffAction : public plansys2::ActionExecutorClient
{
public:
  DropOffAction() : plansys2::ActionExecutorClient("drop-off", 250ms)
  {
    progress_ = 0.0f;
  }

private:
  void do_work() override
  {
    if (progress_ < 1.0f) {
      progress_ += 0.10f;
      send_feedback(progress_, "dropping off package...");
    } else {
      finish(true, 1.0f, "drop-off completed");
      progress_ = 0.0f;
    }
  }

  float progress_;
};

int main(int argc, char ** argv)
{
  rclcpp::init(argc, argv);
  auto node = std::make_shared<DropOffAction>();
  node->set_parameter(rclcpp::Parameter("action_name", "drop-off"));
  node->trigger_transition(
    lifecycle_msgs::msg::Transition::TRANSITION_CONFIGURE);
  node->trigger_transition(
    lifecycle_msgs::msg::Transition::TRANSITION_ACTIVATE);
  rclcpp::spin(node->get_node_base_interface());
  rclcpp::shutdown();
  return 0;
}
\end{lstlisting}

\subsubsection{CMakeLists.txt}

\begin{lstlisting}[language={}, caption={CMakeLists.txt for the transport\_task package.},
    label=lst:cmake]
cmake_minimum_required(VERSION 3.8)
project(transport_task)

find_package(ament_cmake REQUIRED)
find_package(rclcpp REQUIRED)
find_package(rclcpp_lifecycle REQUIRED)
find_package(lifecycle_msgs REQUIRED)
find_package(plansys2_executor REQUIRED)

set(DEPS
  rclcpp
  rclcpp_lifecycle
  lifecycle_msgs
  plansys2_executor
)

add_executable(navigate_action_node src/navigate_action_node.cpp)
ament_target_dependencies(navigate_action_node ${DEPS})

add_executable(pick_up_action_node src/pick_up_action_node.cpp)
ament_target_dependencies(pick_up_action_node ${DEPS})

add_executable(drop_off_action_node src/drop_off_action_node.cpp)
ament_target_dependencies(drop_off_action_node ${DEPS})

install(TARGETS
  navigate_action_node
  pick_up_action_node
  drop_off_action_node
  DESTINATION lib/${PROJECT_NAME}
)

install(DIRECTORY pddl launch config
  DESTINATION share/${PROJECT_NAME}
)

ament_package()
\end{lstlisting}

\subsubsection{Launch File}

\begin{lstlisting}[language={}, caption={Python launch file that starts PlanSys2 core nodes,
loads the PDDL domain, and launches the three action executors
(\texttt{launch/transport\_task.launch.py}).}, label=lst:launch]
import os
from ament_index_python.packages import get_package_share_directory
from launch import LaunchDescription
from launch_ros.actions import Node
from plansys2_support_py.launch import generate_plansys2_launch

def generate_launch_description():
    pkg_dir = get_package_share_directory('transport_task')
    domain_file = os.path.join(pkg_dir, 'pddl', 'transport_domain.pddl')

    plansys2_nodes = generate_plansys2_launch(domain_file=domain_file)

    navigate_node = Node(
        package='transport_task',
        executable='navigate_action_node',
        name='navigate_action_node',
        output='screen',
    )

    pick_up_node = Node(
        package='transport_task',
        executable='pick_up_action_node',
        name='pick_up_action_node',
        output='screen',
    )

    drop_off_node = Node(
        package='transport_task',
        executable='drop_off_action_node',
        name='drop_off_action_node',
        output='screen',
    )

    return LaunchDescription(
        plansys2_nodes + [navigate_node, pick_up_node, drop_off_node]
    )
\end{lstlisting}

\subsubsection{Building and Running}

After placing the package in a ROS2 workspace (\texttt{colcon} workspace), the build and
activation sequence follows the standard ROS2 workflow. First, the workspace is built with
\texttt{colcon build -{}-symlink-install}, and the environment is sourced with
\texttt{source install/setup.bash}. The system is then launched with
\texttt{ros2 launch transport\_task transport\_task.launch.py}. Once running, the
problem instance (objects, predicates, and goal) is loaded into the Problem Manager via the
PlanSys2 terminal client or through programmatic ROS2 service calls, and planning is triggered.

\subsubsection{Expected Execution Behavior}

Figure~\ref{fig:execflow} illustrates the expected action sequence produced by the POPF solver
and dispatched by the PlanSys2 Executor for this problem instance. Because the robot can only
carry one package at a time (\texttt{free} predicate prevents concurrent pick-up), the plan
serializes the two delivery sub-tasks.

% ------------------------------------------------------------------
% FIGURE 4: Plan Execution Sequence  (FIXED — two clearly separated rows,
%           wider boxes, explicit routing, goal marker beside last actions)
% ------------------------------------------------------------------
\begin{figure}[H]
\centering
\resizebox{\linewidth}{!}{%
\begin{tikzpicture}[
    act/.style={
        rectangle, draw=darkblue, fill=lightblue!35, rounded corners=4pt,
        minimum width=3.4cm, minimum height=1.0cm,
        font=\scriptsize\bfseries, align=center, inner sep=5pt
    },
    nav/.style={
        rectangle, draw=medblue, fill=blue!10, rounded corners=4pt,
        minimum width=3.4cm, minimum height=1.0cm,
        font=\scriptsize, align=center, inner sep=5pt
    },
    succ/.style={
        circle, draw=codegreen!70, fill=codegreen!20,
        minimum size=0.7cm, font=\scriptsize\bfseries
    },
    arr/.style={-{Stealth[length=5pt]}, thick, darkgray},
    tlbl/.style={font=\scriptsize, darkgray},
    rlbl/.style={font=\tiny\itshape, darkgray},
]

% ---- Row 1: Parcel 1 (y = 0) ----
\node[act] (pk1)  at ( 0.0,  0) {pick-up\\robot1 parcel1\\warehouse};
\node[nav] (nav1) at ( 4.4,  0) {navigate\\warehouse$\to$\\loading-bay};
\node[nav] (nav2) at ( 8.8,  0) {navigate\\loading-bay$\to$\\storage};
\node[act] (dr1)  at (13.2,  0) {drop-off\\robot1 parcel1\\storage};

% ---- Row 2: Parcel 2 (y = -2.8) ----
\node[act] (pk2)  at ( 0.0, -2.8) {pick-up\\robot1 parcel2\\warehouse};
\node[nav] (nav3) at ( 4.4, -2.8) {navigate\\warehouse$\to$\\loading-bay};
\node[act] (dr2)  at ( 8.8, -2.8) {drop-off\\robot1 parcel2\\loading-bay};
\node[nav] (nav4) at (13.2, -2.8) {navigate\\loading-bay$\to$\\warehouse};

% Goal marker to the right
\node[succ] (g) at (15.6, -1.4) {\checkmark};

% Timing labels (above row 1)
\node[tlbl] at ( 0.0,  0.85) {$t=0$\,s};
\node[tlbl] at ( 4.4,  0.85) {$t=5$\,s};
\node[tlbl] at ( 8.8,  0.85) {$t=15$\,s};
\node[tlbl] at (13.2,  0.85) {$t=25$\,s};

% Row 1 sequential flow
\draw[arr] (pk1)  -- (nav1);
\draw[arr] (nav1) -- (nav2);
\draw[arr] (nav2) -- (dr1);

% Row 2 sequential flow
\draw[arr] (pk2)  -- (nav3);
\draw[arr] (nav3) -- (dr2);
\draw[arr] (dr2)  -- (nav4);

% Cross-row link: after drop-off P1, go back to pick up P2
% Route: dr1.south -> down -> left -> pk2.north
\draw[arr] (dr1.south) -- ++(0,-0.6) -- ++(-13.2,0) -- (pk2.north)
           node[near start, below, rlbl]{reposition to warehouse};

% Both rows converge to goal
\draw[arr] (dr1.east)  -- ++(0.6,0) |- (g.north);
\draw[arr] (nav4.east) -- ++(0.6,0) |- (g.south);

\node[rlbl, codegreen!70!black, above=0.15cm of g] {goal};
\end{tikzpicture}
}% end resizebox
\caption{Expected plan execution sequence for the transport task. Row~1 handles parcel1
(deliver to storage); Row~2 handles parcel2 (deliver to loading-bay, return robot home).
Durations: \texttt{pick-up}/\texttt{drop-off}~=~5\,s; \texttt{navigate}~=~10\,s.
The tick mark indicates goal achievement.}
\label{fig:execflow}
\end{figure}

% ------------------------------------------------------------------
% FIGURE 5: World State Sequence  (FIXED — grid layout, no overlapping arrows)
% ------------------------------------------------------------------
\begin{figure}[H]
\centering
\begin{tikzpicture}[
    st/.style={
        rectangle, draw=darkblue, fill=lightblue!20, rounded corners=4pt,
        minimum width=2.8cm, minimum height=1.1cm,
        font=\scriptsize, align=center, inner sep=6pt
    },
    stg/.style={
        rectangle, draw=codegreen!80, fill=codegreen!15, rounded corners=4pt,
        minimum width=2.8cm, minimum height=1.1cm,
        font=\scriptsize, align=center, inner sep=6pt
    },
    arr/.style={-{Stealth[length=5pt]}, thick, darkgray},
    lbl/.style={font=\scriptsize\itshape, darkgray, align=center},
]

% Row 1 (top): s0 -> s1 -> s2 -> s3  (left to right, y=0)
\node[st] (s0) at ( 0.0, 0.0) {$s_0$\\robot@wh\\P1,P2@wh};
\node[st] (s1) at ( 3.8, 0.0) {$s_1$\\robot@wh\\carrying P1};
\node[st] (s2) at ( 7.6, 0.0) {$s_2$\\robot@lb\\carrying P1};
\node[st] (s3) at (11.4, 0.0) {$s_3$\\robot@st\\carrying P1};

% Row 2 (bottom): s4 -> s5 -> s6 -> s7  (right to left, y=-2.4)
\node[st] (s4) at (11.4,-2.4) {$s_4$\\robot@st\\P1@st, free};
\node[st] (s5) at ( 7.6,-2.4) {$s_5$\\robot@wh\\P2@wh, free};
\node[st] (s6) at ( 3.8,-2.4) {$s_6$\\robot@wh\\carrying P2};
\node[st] (s7) at ( 0.0,-2.4) {$s_7$\\robot@lb\\carrying P2};

% Goal state below s7
\node[stg] (sg) at ( 0.0,-4.8) {$s_{\!G}$\\P1@st, P2@lb\\robot@wh};

% Top row arrows
\draw[arr] (s0) -- (s1) node[midway,above,lbl]{pick-up P1};
\draw[arr] (s1) -- (s2) node[midway,above,lbl]{nav$\to$lb};
\draw[arr] (s2) -- (s3) node[midway,above,lbl]{nav$\to$st};

% Turn corner: s3 down to s4
\draw[arr] (s3) -- (s4) node[midway,right,lbl]{drop-off P1};

% Bottom row arrows (right to left)
\draw[arr] (s4) -- (s5) node[midway,above,lbl]{nav$\to$wh};
\draw[arr] (s5) -- (s6) node[midway,above,lbl]{pick-up P2};
\draw[arr] (s6) -- (s7) node[midway,above,lbl]{nav$\to$lb};

% s7 down to goal
\draw[arr] (s7) -- (sg) node[midway,right,lbl]{drop-off P2};

\node[font=\scriptsize, codegreen!70!black, right=0.2cm of sg] {goal reached};

\node[font=\tiny,darkgray] at (5.7, -5.5)
    {\textit{wh}=warehouse \quad \textit{lb}=loading-bay \quad \textit{st}=storage};
\end{tikzpicture}
\caption{World state sequence corresponding to the transport plan. Each state transition
corresponds to one PDDL action execution. The path snakes: top row left-to-right, then
bottom row right-to-left, ending at the goal state $s_G$.}
\label{fig:statespace}
\end{figure}

\subsection{Known Limitations of PlanSys2}

Despite its practical utility, PlanSys2 exhibits limitations that are directly relevant to this
research:

\textbf{Epistemic limitations.} PlanSys2 is fundamentally a classical single-agent planning model.
The world state is a set of ground predicates assumed to be fully known to the planner. There is
no representation of agent-relative knowledge, no epistemic goal language, and no support for
actions that change knowledge states without changing the physical world.

\textbf{Partial observability.} PlanSys2 does not natively handle partial observability. Sensor
uncertainty and missing observations must be handled outside the planner before updating
predicates. The planner always assumes predicates reflect the true world state.

\textbf{Multi-agent coordination.} PlanSys2 is designed for single-robot planning. While multiple
instances can run in parallel, there is no native mechanism for cross-robot plan coordination,
shared world state, or multi-agent goal management.

\textbf{Solver expressivity.} The solvers supported by PlanSys2 (POPF, TFD) implement classical
or temporal planning with no support for epistemic or probabilistic reasoning. Extending PlanSys2
to use an epistemic planner would require a new solver plugin and a restructured problem
representation.

These limitations motivate the core research objective: to integrate the engineering infrastructure
of ROS2 and PlanSys2 with the expressive semantics of DEL-based epistemic planning.


\newpage
\section{Multi-Robot Systems in ROS2}
\label{sec:multirobot}


\subsection{Architectural Patterns for Multi-Robot Systems}

Multi-robot systems in ROS2 are typically organized according to one of three architectural
patterns: \textit{centralized}, \textit{decentralized}, or \textit{hierarchical}. In a centralized
architecture, a single master node maintains a global world model and generates plans for all
robots. In a decentralized architecture, each robot operates as an independent planning agent,
communicating with peers to resolve conflicts and share observations. A hierarchical
architecture---arguably the most practically relevant for the target research---combines a
fleet-level planner with robot-level planners, where the fleet planner decomposes high-level goals
into sub-goals assigned to individual robots.

\subsection{Multi-Robot Navigation and Coordination}

Navigation in multi-robot ROS2 systems is typically managed by the Nav2 stack, supporting
namespaced multi-robot deployments. Open-RMF (Open Robotics Middleware Framework) provides a
standardized interface for multi-fleet, multi-robot task allocation and coordination in shared
environments~\cite{openrmf2021}.

\subsection{Task Allocation and Multi-Agent Planning}

Task allocation assigns tasks to robots to optimize objectives such as makespan, coverage, or load
balance. Classical approaches include market-based auction methods and integer programming
formulations. In the context of PlanSys2, one approach is to model multiple robot agents as PDDL
objects in a single planning instance, leveraging MA-PDDL or MASTRIPS extensions. However, such
approaches remain within the classical planning paradigm and inherit all its epistemic limitations.

\subsection{Shared World Modeling}

A fundamental challenge is maintaining a shared, consistent world model across all agents. From
an epistemic planning perspective, the problem is more subtle: it is not always desirable or
possible to maintain a single globally consistent model. Different robots may have genuinely
different beliefs about the state of the world, and those differences are semantically
meaningful. An epistemic planner must represent and reason about individual perspectives, plan
actions that resolve epistemic differences, and formulate goals referring explicitly to agent
knowledge states.

% ------------------------------------------------------------------
% FIGURE 6: Multi-Robot System  (FIXED — triangular layout, no overlapping nodes)
% ------------------------------------------------------------------
\begin{figure}[H]
\centering
\begin{tikzpicture}[
    robot/.style={
        circle, draw=darkblue, fill=lightblue!30,
        minimum size=1.2cm, font=\small\bfseries, inner sep=3pt
    },
    kb/.style={
        rectangle, draw=medblue, fill=blue!5, rounded corners=3pt,
        minimum width=2.2cm, minimum height=0.9cm,
        font=\scriptsize, align=center, inner sep=5pt
    },
    fleet/.style={
        rectangle, draw=codered!70, fill=codered!5, rounded corners=5pt,
        minimum width=4.4cm, minimum height=1.1cm,
        font=\small\bfseries, align=center, inner sep=8pt
    },
    arr/.style={-{Stealth[length=5pt]}, thick},
    obs/.style={-{Stealth[length=5pt]}, dashed, gray},
    lbl/.style={font=\tiny, darkgray},
]

% Fleet manager at top centre
\node[fleet] (fm) at (4.0, 3.6) {Fleet Manager /\\Epistemic Planner};

% Three robots in a triangle: R1 left, R2 right, R3 bottom centre
\node[robot] (r1) at (0.5, 0.8) {R1};
\node[robot] (r2) at (7.5, 0.8) {R2};
\node[robot] (r3) at (4.0,-1.8) {R3};

% Knowledge bases below/beside each robot
\node[kb] (kb1) at (0.5, -0.8) {$K_{R1}$\\local beliefs};
\node[kb] (kb2) at (7.5, -0.8) {$K_{R2}$\\local beliefs};
\node[kb] (kb3) at (4.0, -3.2) {$K_{R3}$\\local beliefs};

% Fleet manager <-> robots (goal down, obs up)
\draw[arr, darkblue]
    (fm.west) -- ++(-1.0,0) |- (r1.north)
    node[near end, left, lbl]{goal};
\draw[arr, darkblue]
    (fm.east) -- ++(1.0,0) |- (r2.north)
    node[near end, right, lbl]{goal};
\draw[arr, darkblue]
    (fm.south) -- (r3.north)
    node[midway, right, lbl]{goal};

\draw[arr, gray]
    (r1.north) -- ++(0,0.4) -- (fm.west |- r1.north) -- (fm.west)
    node[near start, right, lbl]{obs};
\draw[arr, gray]
    (r2.north) -- ++(0,0.4) -- (fm.east |- r2.north) -- (fm.east);
\draw[arr, gray]
    (r3.north) to[bend left=10] (fm.south);

% Robot -> local KB
\draw[obs] (r1) -- (kb1);
\draw[obs] (r2) -- (kb2);
\draw[obs] (r3) -- (kb3);

% Peer-to-peer communication between robots
\draw[arr, dashed, codered!70]
    (r1.east) to[bend left=18] node[midway, above, lbl]{comm.} (r2.west);
\draw[arr, dashed, codered!70]
    (r2.west) to[bend left=18] (r1.east);
\draw[arr, dashed, codered!70]
    (r1.south east) to[bend right=12] (r3.north west);
\draw[arr, dashed, codered!70]
    (r3.north east) to[bend right=12] (r2.south west);
\end{tikzpicture}
\caption{Multi-robot system with a fleet-level epistemic planner and robot-level local knowledge
bases. Robots send observations to the fleet planner and engage in peer-to-peer communication,
giving rise to heterogeneous epistemic states.}
\label{fig:multirobot}
\end{figure}


\newpage
\section{Dynamic Epistemic Logic: Foundations for Epistemic Planning}
\label{sec:del}


\subsection{Motivation: From Classical to Epistemic Planning}

Classical planning operates over a deterministic, fully observable world. It cannot represent
the knowledge states of individual agents, uncertainty about the current state, or the epistemic
effects of communication. Dynamic Epistemic Logic (DEL)~\cite{baltag1998logic, vanditmarsch2007}
provides a rigorous formal framework for reasoning about knowledge, belief, and informational
change in multi-agent systems, extending classical propositional logic with modal operators and
a formal semantics for how truth values change when epistemic actions are applied.

\subsection{Epistemic States and Kripke Semantics}

\begin{definition}[Multi-Agent Epistemic Model]
A multi-agent epistemic model is a triple $M = (W, R, L)$ where:
\begin{itemize}[noitemsep]
\item $W \neq \emptyset$ is a finite set of \textit{possible worlds};
\item $R : \mathrm{Ag} \to 2^{W \times W}$ assigns to each agent $i$ an
      \textit{accessibility relation} $R_i \subseteq W \times W$; and
\item $L : W \to 2^P$ assigns to each world a \textit{label}---the set of atoms true there.
\end{itemize}
An epistemic state is a pair $s = (M, W_d)$ where $W_d \subseteq W$ is a non-empty set of
\textit{designated worlds}.
\end{definition}

The accessibility relation $w R_i v$ means that in world $w$, agent $i$ considers $v$ possible
given its current observations. A formula $\Box_i \varphi$ (``agent $i$ knows $\varphi$'') holds
in $w$ iff $\varphi$ holds in all $R_i$-accessible worlds:
\[
(M, w) \models \Box_i \varphi \iff \forall v \in W: w R_i v \Rightarrow (M, v) \models \varphi
\]

The \textit{knowing-whether} modality $\mathrm{Kw}_i \varphi = \Box_i \varphi \vee \Box_i
\neg\varphi$ is especially important in robotics, where sensors resolve binary queries.

\subsection{Common Knowledge}

For a group $G \subseteq \mathrm{Ag}$, \textit{common knowledge} $C_G \varphi$ requires that all
agents know $\varphi$, all agents know that all know it, and so on ad infinitum:
\[
C_G \varphi = \bigwedge_{k > 0} \Box_G^k \varphi,
\quad \Box_G \varphi = \bigwedge_{i \in G} \Box_i \varphi
\]

Common knowledge is essential for robotic coordination: a team that must act in concert requires
not only that each robot knows the plan, but that it is common knowledge among all robots that
they know the plan---otherwise a robot might fail to act because it is uncertain whether its
teammates are aware of their roles.

\subsection{Epistemic Actions and the Product Update}

\begin{definition}[Event Model and Epistemic Action]
An event model is a quadruple $A = (E, Q, \mathrm{pre}, \mathrm{post})$ where $E \neq \emptyset$
is a finite set of \textit{events}; $Q : \mathrm{Ag} \to 2^{E \times E}$ assigns each agent an
accessibility relation over events; $\mathrm{pre} : E \to \mathcal{L}^C_{P,\mathrm{Ag}}$ assigns
preconditions; and $\mathrm{post} : E \times P \to \mathcal{L}^C_{P,\mathrm{Ag}}$ assigns
postconditions. An epistemic action is a pair $a = (A, E_d)$ where $E_d \subseteq E$ are the
\textit{designated events}.
\end{definition}

\begin{definition}[Product Update]
Let $a = ((E, Q, \mathrm{pre}, \mathrm{post}), E_d)$ be applicable in state
$s = ((W, R, L), W_d)$. The product update $s \otimes a = ((W', R', L'), W'_d)$ is:
\begin{align*}
W' &= \{(w, e) \in W \times E \mid (M, w) \models \mathrm{pre}(e)\} \\
R'_i &= \{((w, e), (v, f)) \mid w R_i v \text{ and } e Q_i f\} \\
L'(w, e) &= \{p \in P \mid (M, w) \models \mathrm{post}(e, p)\} \\
W'_d &= \{(w, e) \in W' \mid w \in W_d,\, e \in E_d\}
\end{align*}
\end{definition}

The product update simultaneously captures physical effects (through postconditions) and epistemic
effects (through combined world and event accessibility relations). An agent's new beliefs are
determined by what it knew before combined with what it can observe about the action.

\subsection{Types of Epistemic Actions}

A \textit{public announcement} $\mathrm{ann}(\varphi)$ communicates a formula to all agents
simultaneously with a reflexive accessibility relation. It is the simplest DEL action and forms
the basis of the Public Announcement Logic (PAL) fragment.

A \textit{private ontic action} is one where the actor moves a physical object while other agents
are oblivious---modeled with a ``null event'' linked to the real event via non-actor accessibility.

A \textit{sensing action} allows agents to observe a binary property, with two designated events
for each outcome. After sensing, a fully observant agent knows whether the property holds.

A \textit{quasi-private sensing action} combines private and semi-private observability: some
agents are fully observant, some know only that sensing occurred, and some are oblivious.

\subsection{Epistemic Planning Tasks}

\begin{definition}[Epistemic Planning Task]
An epistemic planning task is a triple $T = (s_0, \mathrm{Act}, \varphi_g)$ where $s_0$ is an
initial epistemic state, $\mathrm{Act}$ is a finite set of epistemic actions, and $\varphi_g$ is
a goal formula. A solution is a sequence $\pi = a_1, \ldots, a_l$ such that $\pi$ is applicable
in $s_0$ and $s_0 \otimes \pi \models \varphi_g$.
\end{definition}

Crucially, $\varphi_g$ may be an epistemic formula: for example,
$\Box_{R_1}\mathrm{packageDelivered} \wedge \mathrm{Kw}_{R_2}\mathrm{corridorBlocked}$
specifies knowledge states as goals---not expressible in classical PDDL.


\newpage
\section{EPDDL: The Epistemic Planning Domain Definition Language}
\label{sec:epddl}

\subsection{Motivation and Design Philosophy}

EPDDL, introduced by Burigana and Fabiano~\cite{burigana2026epddl} as the official language for
the Epistemic Planning Track at IPC 2026, addresses the absence of a standardized language for
DEL-based epistemic planning problems. It is designed for: (1)~compatibility with PDDL syntax;
(2)~formal grounding in full DEL semantics with abstract event models; and (3)~a modular action
type library system enabling specification and comparison of DEL fragments.

\subsection{Language Structure}

An EPDDL specification consists of three components: \textbf{problems} (instance-specific:
objects, agents, initial epistemic state, goal); \textbf{domains} (instance-independent: type
declarations, predicates, event definitions, action schemas); and \textbf{action type libraries}
(abstract frames defining observability patterns for different DEL fragments).

\subsection{EPDDL Syntax: A Concrete Example}

We present a simplified domain and problem for a two-robot corridor-sensing scenario: robots
\texttt{r1} and \texttt{r2} are adjacent to corridor \texttt{c1}, whose blocked status is
initially unknown to both. Robot \texttt{r1} can sense the corridor and then announce its finding.
The goal is that all agents know whether the corridor is blocked.

\subsubsection{EPDDL Domain}

\begin{lstlisting}[language={}, caption={EPDDL domain for a two-robot sensing and announcement
scenario (\texttt{corridor\_sensing.epddl}).}, label=lst:epddldomain]
(define (domain corridor-sensing)
  (:requirements :typing :sensing :announcements :group-modalities)

  (:types robot corridor)

  (:predicates
    (blocked ?c - corridor)
    (adjacent ?r - robot ?c - corridor)
  )

  (:action sense-corridor
    :type sensing
    :actor ?r - robot
    :parameters (?r - robot ?c - corridor)
    :observability (
      (full-obs ?r)
      (no-obs All \ {?r})
    )
    :precondition (adjacent ?r ?c)
    :sense-formula (blocked ?c)
  )

  (:action announce-blocked
    :type public-announcement
    :actor ?r - robot
    :parameters (?r - robot ?c - corridor)
    :precondition (and
      (adjacent ?r ?c)
      ([?r] (blocked ?c))
    )
    :announced-formula (blocked ?c)
  )

  (:action announce-clear
    :type public-announcement
    :actor ?r - robot
    :parameters (?r - robot ?c - corridor)
    :precondition (and
      (adjacent ?r ?c)
      ([?r] (not (blocked ?c)))
    )
    :announced-formula (not (blocked ?c))
  )
)
\end{lstlisting}

\subsubsection{EPDDL Problem}

\begin{lstlisting}[language={}, caption={EPDDL problem instance using a finitary S5-theory
for the initial epistemic state (\texttt{corridor\_task.epddl}).}, label=lst:epddlproblem]
(define (problem corridor-task-1)
  (:domain corridor-sensing)

  (:objects
    r1 r2 - robot
    c1 - corridor
  )

  (:agents r1 r2)

  (:init
    (:static
      (adjacent r1 c1)
      (adjacent r2 c1)
    )
    (:epistemic
      (C All (Kw r1 (blocked c1)) false)
      (C All (Kw r2 (blocked c1)) false)
    )
  )

  (:goal (and
    ([Kw. All] (blocked c1))
  ))
)
\end{lstlisting}

The \texttt{:epistemic} block encodes a finitary S5-theory:
\texttt{(C All (Kw r1 (blocked c1)) false)} asserts that it is common knowledge among all agents
that \texttt{r1} does not know whether the corridor is blocked. The goal
\texttt{([Kw. All] (blocked c1))} uses the group knowing-whether modality to require all agents
to resolve the uncertainty. Key syntactic constructs are summarized in
Table~\ref{tab:epddlsyntax}.

\begin{table}[H]
\centering
\begin{tabular}{lll}
\toprule
\textbf{EPDDL Construct} & \textbf{Logical Meaning} & \textbf{Context} \\
\midrule
\texttt{([i] phi)}           & $\Box_i \varphi$ --- agent $i$ knows $\varphi$        & preconditions, goals \\
\texttt{(<i> phi)}           & $\Diamond_i \varphi$ --- agent $i$ considers possible  & preconditions \\
\texttt{([Kw. i] phi)}       & $\mathrm{Kw}_i \varphi$ --- knows whether $\varphi$   & goals, init \\
\texttt{([Kw. All] phi)}     & $\mathrm{Kw}_\mathrm{All} \varphi$ --- all know whether & goals \\
\texttt{(C All phi)}         & $C_\mathrm{All} \varphi$ --- common knowledge          & init, goals \\
\texttt{:type sensing}       & sensing action type                                    & action schema \\
\texttt{:type public-announcement} & public announcement type                         & action schema \\
\texttt{:sense-formula}      & formula whose truth is observed                        & sensing actions \\
\texttt{:announced-formula}  & formula publicly communicated                          & announcement actions \\
\texttt{:observability}      & per-type observability assignment                      & action schema \\
\bottomrule
\end{tabular}
\caption{Key EPDDL syntactic constructs with logical counterparts and usage contexts.}
\label{tab:epddlsyntax}
\end{table}

\subsection{Modal Operators in EPDDL}

\begin{table}[H]
\centering
\begin{tabular}{lll}
\toprule
\textbf{EPDDL Syntax} & \textbf{Logical Formula} & \textbf{Reading} \\
\midrule
\texttt{([i] phi)}    & $\Box_i \varphi$               & Agent $i$ knows/believes $\varphi$ \\
\texttt{(<i> phi)}    & $\Diamond_i \varphi$           & Agent $i$ considers $\varphi$ possible \\
\texttt{([Kw. i] phi)}& $\mathrm{Kw}_i \varphi$        & Agent $i$ knows whether $\varphi$ \\
\texttt{(<Kw. i> phi)}& $\widehat{\mathrm{Kw}}_i\varphi$ & Agent $i$ does not know whether $\varphi$ \\
\texttt{([C. G] phi)} & $C_G \varphi$                  & $\varphi$ is common knowledge in $G$ \\
\texttt{(<C. G> phi)} & $\hat{C}_G \varphi$             & Dual of common knowledge \\
\bottomrule
\end{tabular}
\caption{Modal operators in EPDDL and their logical counterparts.}
\label{tab:modalities}
\end{table}

Modality indices may be single agent terms, agent group lists, or the reserved keyword
\texttt{All}. Group modalities require the \texttt{:group-modalities} requirement flag.

\subsection{Initial State Representation}

EPDDL supports two methods for specifying the initial epistemic state. An \textit{explicit
definition} directly encodes worlds, accessibility relations, world labels, and designated worlds.
A \textit{finitary S5-theory}~\cite{son2014finitary} specifies the state through commonly-known
epistemic formulas of restricted form, consisting of propositional atoms describing the actual
world, $C_\mathrm{Ag}(\Box_i \varphi)$ formulas for commonly-known truths,
$C_\mathrm{Ag}(\mathrm{Kw}_i \varphi)$ formulas for agent certainties, and
$C_\mathrm{Ag}(\widehat{\mathrm{Kw}}_i \varphi)$ formulas for agent uncertainties. The
\texttt{plank} toolkit automatically constructs the Kripke model from a finitary S5-theory.

\subsection{DEL Fragments via Action Type Libraries}

\begin{figure}[H]
\centering
\begin{tikzpicture}[
    frag/.style={
        rectangle, draw=darkblue, fill=blue!8, rounded corners=5pt,
        minimum width=3.4cm, minimum height=1.0cm,
        align=center, font=\small, inner sep=7pt
    },
    subfrag/.style={
        rectangle, draw=medblue, fill=blue!15, rounded corners=4pt,
        minimum width=3.0cm, minimum height=0.9cm,
        align=center, font=\scriptsize, inner sep=6pt
    },
    arr/.style={-{Stealth[length=5pt]}, thick, darkgray}
]
\node[frag] (del) at (4.0, 0) {Full DEL\\(EPDDL)};

\node[subfrag] (po)      at (0.0, -2.2) {Partial Obs.\\Actions};
\node[subfrag] (ontic)   at (4.0, -2.2) {Ontic\\Actions};
\node[subfrag] (sensing) at (8.0, -2.2) {Sensing\\Actions};

\node[subfrag] (pal)  at (2.0, -4.4) {PAL\\(Baseline)};
\node[subfrag] (pall) at (6.0, -4.4) {PALL+\\Ontic};

\draw[arr] (del.south west) -- (po.north);
\draw[arr] (del.south)      -- (ontic.north);
\draw[arr] (del.south east) -- (sensing.north);

\draw[arr] (po.south)      -- (pal.north);
\draw[arr] (ontic.south)   -- (pall.north);
\draw[arr] (sensing.south) -- (pall.north);
\end{tikzpicture}
\caption{Hierarchy of DEL fragments expressible through EPDDL action type libraries. The PAL
baseline supports only public announcements. Richer fragments add ontic actions, partial
observability, and sensing actions.}
\label{fig:delfragments}
\end{figure}

Action type libraries allow benchmark designers to create problem sets solvable by planners of
different capabilities. Any epistemic planning formalism translatable into a DEL fragment can be
represented in EPDDL via an appropriate library, enabling cross-formalism comparison.

\subsection{Abstract Event Models and Action Types}

The key theoretical innovation is the \textit{abstract epistemic action}, which decouples the
structural pattern of observability (action type) from specific preconditions, effects, and agent
assignments (domain action).

\begin{definition}[Abstract Event Model]
An abstract event model on $\mathrm{ObsTypes}$ is a tuple $(E, Q, \mathrm{pre}, \mathrm{post},
\mathrm{obs})$ where $(E, Q)$ is an abstract frame, $\mathrm{pre}$ and $\mathrm{post}$ are as
in a standard event model, and $\mathrm{obs} : \mathrm{Ag} \to (\mathrm{ObsTypes} \to
\mathcal{L}^C_{P,\mathrm{Ag}})$ assigns to each agent an observability function satisfying
mutual inconsistency and coverage of the logical space.
\end{definition}

\newpage
\section{Integration: Toward a DEL-Based Planning System for ROS2}
\label{sec:integration}

\subsection{Conceptual Architecture}

Integrating DEL-based epistemic planning with a ROS2 multi-robot system requires bridging three
conceptual gaps: (i) between the physical robot state and the epistemic state representation;
(ii) between EPDDL action schemas and ROS2 action server implementations; and (iii) between the
planner's output---a sequence of abstract epistemic actions---and the robot execution
infrastructure.

We propose a layered conceptual architecture composed of three interacting strata:
a \emph{Cognitive Layer}, an \emph{Execution Layer}, and a \emph{Perception Layer}.
This separation clarifies the interaction between symbolic epistemic reasoning and
physical multi-robot control.

\textbf{Cognitive Layer.}
This layer maintains and manipulates the global epistemic state $(M, W_d)$ of the fleet.
It contains:

\begin{itemize}
\item \textbf{Epistemic World Model Manager:} Maintains the Kripke model and applies
product update semantics (or suitable approximations) upon perception events or
action completion.

\item \textbf{EPDDL Planner Interface:} Generates EPDDL problem instances from the
current epistemic state, invokes an external epistemic planner, and translates the
resulting abstract plan into dispatchable actions.

\item \textbf{Epistemic Action Dispatcher:} Instantiates abstract epistemic actions
as physical or communication events, dispatches them to ROS2, and triggers the
corresponding epistemic product update.
\end{itemize}

\textbf{Execution Layer.}
Implements low-level robot behaviors using standard ROS2 action servers
(navigation, manipulation, sensing, communication). This layer is responsible for
physical actuation and execution monitoring.

\textbf{Perception Layer.}
Transforms physical sensor data into symbolic assertions and epistemic events.
A state estimation and belief update module translates observations into grounded
propositions and applies updates to the global epistemic state.


\begin{figure*}[t]
\centering
\resizebox{\textwidth}{!}{%
\begin{tikzpicture}[
    transform shape,
    node distance=2.8cm and 3.8cm,
    comp/.style={
        rectangle, draw=darkblue, fill=lightblue!25,
        rounded corners=6pt,
        minimum width=4.2cm,
        minimum height=1.2cm,
        font=\small\bfseries,
        align=center
    },
    ext/.style={
        rectangle, draw=codered!70, fill=codered!10,
        rounded corners=6pt,
        minimum width=4.2cm,
        minimum height=1.2cm,
        font=\small\bfseries,
        align=center
    },
    layer/.style={
        rectangle,
        draw=black!50,
        rounded corners=10pt,
        inner sep=10pt,
        thick
    },
    plan/.style={-{Stealth[length=5pt,width=4pt]}, thick, darkblue},
    sensor/.style={-{Stealth[length=5pt,width=4pt]}, thick, darkgray, dashed},
    lbl/.style={
        font=\scriptsize,
        fill=white,
        inner sep=2pt
    }
]

\node[comp] (ewm) {Epistemic World\\Model Manager};
\node[comp, right=of ewm] (dispatcher)
    {Epistemic Action\\Dispatcher};
\node[comp, right=of dispatcher] (ros2)
    {ROS2 Action Servers};
\node[comp, right=of ros2] (robots)
    {Robot Fleet};

\node[comp, above=2.6cm of dispatcher] (planner)
    {EPDDL Planner\\Interface};

\node[ext, right=3.2cm of planner] (solver)
    {External\\Epistemic Solver};

\node[comp, below=2.6cm of dispatcher] (estimation)
    {State Estimation\\\& Belief Update};


\node[layer,
      fit=(ewm)(dispatcher)(planner)(solver),
      label={[anchor=north west,
              font=\bfseries,
              yshift=4pt]north west:Cognitive Layer}] {};

\node[layer,
      fit=(ros2)(robots),
      label={[anchor=north west,
              font=\bfseries,
              yshift=4pt]north west:Execution Layer}] {};

\node[layer,
      fit=(estimation),
      label={[anchor=north west,
              font=\bfseries,
              yshift=4pt]north west:Perception Layer}] {};


\draw[plan]
    (ewm.north) -- (planner.west)
    node[midway, above, lbl]{epistemic state};

\draw[plan]
    (planner.east) -- (solver.west)
    node[midway, above, lbl]{EPDDL problem};

\draw[plan]
    (solver.west) -- (planner.east)
    node[midway, below, lbl]{plan};

\draw[plan]
    (planner.south) -- (dispatcher.north)
    node[midway, right, lbl]{action sequence};

\draw[plan]
    (dispatcher.west) -- (ewm.east)
    node[midway, above, lbl]{product update};

\draw[plan]
    (dispatcher.east) -- (ros2.west)
    node[midway, above, lbl]{action goals};

\draw[plan]
    (ros2.east) -- (robots.west)
    node[midway, above, lbl]{commands};

\draw[sensor]
    (robots.south) -- (estimation.north)
    node[midway, right, lbl]{sensor data};

\draw[sensor]
    (estimation.north) -- (ewm.south)
    node[midway, right, lbl]{epistemic update};

\begin{scope}[shift={(2,-5)}]
    \draw[plan] (0,0) -- (2,0)
        node[right=3pt, lbl]{planning / control signal};
    \draw[sensor] (0,-0.6) -- (2,-0.6)
        node[right=3pt, lbl]{sensor / perception data};
\end{scope}

\end{tikzpicture}%
}
\caption{Layered conceptual architecture for DEL-based epistemic multi-robot planning.
The Cognitive Layer maintains and updates the global Kripke model and generates
epistemic plans. The Execution Layer implements physical action through ROS2 action
servers. The Perception Layer translates sensor observations into symbolic
epistemic updates applied via DEL product update semantics.}
\label{fig:layered_architecture}
\end{figure*} \newpage

\subsection{State Space and Computational Challenges}

The number of possible worlds in a Kripke model can grow exponentially with the number of
propositions and agents. Several approximation strategies have been explored: \textit{bounded
world representations} limit the number of worlds using heuristic pruning; \textit{belief space
planning} maintains probability distributions over worlds (POMDP-style), trading epistemic
precision for tractability; and \textit{compiled classical planning
approaches}~\cite{muise2015planning} transform epistemic problems into classical ones by encoding
epistemic state as additional propositional atoms.

\subsection{Perception-to-Epistemic-State Translation}

A critical engineering challenge is translating robot sensor data into Kripke model updates. A
robot's observation---a camera image, a lidar scan---is a physical signal that must be interpreted
as an epistemic event. In the ROS2 context, this pipeline consists of a perception node
(producing symbolic object detections or map assertions), a semantic interpretation layer
(translating detections into PDDL-compatible ground atoms), and an epistemic update module
(applying the product update to the World Model Manager's Kripke model).

\subsection{Communication Actions as Epistemic Events}

Inter-robot communication is itself an epistemic event: when robot $R_1$ broadcasts that corridor
$c_3$ is blocked, this constitutes a public announcement of $\Box_{R_1}\mathrm{corridorBlocked}(c_3)$,
and the epistemic state changes via the DEL product update. In the proposed architecture, the
Epistemic Action Dispatcher implements communication actions as ROS2 topic publications while
simultaneously applying the corresponding product update.

\subsection{Open Research Questions}

\textbf{Tractable epistemic state representations.} What Kripke model representations are both
expressive enough for EPDDL-level reasoning and compact enough for real-time multi-robot fleets?
Are factored or compiled representations sufficient?

\textbf{Online epistemic planning.} What are the performance characteristics of available
epistemic planners (EFP, mGPT, MCEPlan) in online replanning settings?

\textbf{EPDDL-to-ROS2 toolchain.} How should the \texttt{plank} toolkit integrate with the ROS2
build and runtime systems? Can EPDDL problem files be auto-generated from ROS2 system state?

\textbf{Epistemic goal specification.} What user interface abstractions allow operators to specify
goals such as ``ensure all robots know whether the target room is occupied''?

\textbf{Failure handling.} When epistemic plan actions fail, the epistemic state must be updated
and a new plan generated. What recovery strategies are appropriate in a ROS2 runtime?


\newpage
\bibliographystyle{plain}
\begin{thebibliography}{99}

\bibitem{macenski2022robot}
Macenski, S., Foote, T., Gerkey, B., Lalancette, C., \& Woodall, W. (2022).
\textit{Robot Operating System 2: Design, architecture, and uses in the wild}.
Science Robotics, 7(66).

\bibitem{martin2021plansys2}
Mart\'in, F., Mateos, J., Vargas, F., Moreno, R., Fernandez-Rodr\'iguez, J.~C., \& Ginesi, A.
(2021). \textit{PlanSys2: A Planning System Framework for ROS2}.
In Proceedings of the IEEE/RSJ IROS.

\bibitem{baltag1998logic}
Baltag, A., Moss, L.~S., \& Solecki, S. (1998).
\textit{The Logic of Public Announcements, Common Knowledge and Private Suspicions}.
In Proceedings of TARK-98.

\bibitem{vanditmarsch2007}
van Ditmarsch, H., van der Hoek, W., \& Kooi, B.~P. (2007).
\textit{Dynamic Epistemic Logic}. Volume 337. Springer Netherlands.

\bibitem{burigana2026epddl}
Burigana, A., \& Fabiano, F. (2026).
\textit{The Epistemic Planning Domain Definition Language: Official Guideline}.
Official Guideline Reference for the Epistemic Planning Track at IPC-26.

\bibitem{bolander2011epistemic}
Bolander, T., \& Andersen, M.~B. (2011).
\textit{Epistemic Planning for Single- and Multi-Agent Systems}.
Journal of Applied Non-Classical Logics, 21(1), 9--34.

\bibitem{coles2010forward}
Coles, A., Coles, A., Fox, M., \& Long, D. (2010).
\textit{Forward-Chaining Partial-Order Planning}.
In Proceedings of ICAPS.

\bibitem{faconti2018behaviortree}
Faconti, D., \& Colledanchise, M. (2018).
\textit{BehaviorTree.CPP: A Behavior Tree Library for C++}.
Available: \url{https://www.behaviortree.dev}.

\bibitem{openrmf2021}
Open Robotics (2021).
\textit{Open-RMF: Open Robotics Middleware Framework}.
Available: \url{https://www.open-rmf.org}.

\bibitem{muise2015planning}
Muise, C., Belle, V., Felli, P., McIlraith, S.~A., Miller, T., Pearce, A.~R., \&
Sonenberg, L. (2015).
\textit{Planning Over Multi-Agent Epistemic States: A Classical Planning Approach}.
In Proceedings of AAAI 2015.

\bibitem{son2014finitary}
Son, T.~C., Pontelli, E., Baral, C., \& Gelfond, G. (2014).
\textit{Finitary S5-Theories}.
In Proceedings of JELIA 2014. LNCS vol. 8761, Springer.

\bibitem{kominis2015beliefs}
Kominis, F., \& Geffner, H. (2015).
\textit{Beliefs in Multiagent Planning: From One Agent to Many}.
In Proceedings of ICAPS 2015.

\bibitem{hintikka1962knowledge}
Hintikka, J. (1962).
\textit{Knowledge and Belief: An Introduction to the Logic of the Two Notions}.
Cornell University Press.

\bibitem{ghallab2004automated}
Ghallab, M., Nau, D.~S., \& Traverso, P. (2004).
\textit{Automated Planning: Theory and Practice}. Elsevier.

\end{thebibliography}

\end{document}